{
  "scripts": {
    "flow": "flow",
    "build": "flow && flow-remove-types src/ -d out/",
    "defs": "\"$npm_execpath\" run build && cat defs.json | node out/index.js > defs.d.ts"
  },
  "license": "CC0",
  "devDependencies": {
    "flow-bin": "^0.161.0",
    "flow-remove-types": "^2.170.0"
  }
}